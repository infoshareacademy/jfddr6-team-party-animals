const SignUp = () => {
  return (
    <div>
      You are not logged in, you have to log in or register.
      <div>Login</div>
      <div>Create account</div>
      <div>Reset password</div>
    </div>
  );
};

export default SignUp;
